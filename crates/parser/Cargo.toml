[package]
name = "wisp-parser"
version = "0.1.0"
edition = "2024"

[dependencies]
wisp-core = { path = "../core" }
woven-html = "0.1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
regex = "1"
