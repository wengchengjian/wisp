[package]
name = "wisp-stealth"
version = "0.1.0"
edition = "2021"

[dependencies]
wisp-core = { path = "../core" }
wisp-browser = { path = "../browser" }
tokio = { version = "1", features = ["full"] }
serde_json = "1"
rand = "0.10"
tracing = "0.1"
chrono = { version = "0.4", features = ["serde"] }
